@model IEnumerable<InventoryModule.Models.Masters.LedgerGroup>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.LedgerGroupName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LedgerGroupName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LedgerGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LedgerGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LedgerGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
